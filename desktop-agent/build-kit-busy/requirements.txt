# Runtime + build dependencies. build.bat installs these into a local venv.
# Versions chosen so the build works on Python 3.10 through 3.14+.

# Build tools
pyinstaller>=6.15.0

# GUI / tray
pystray>=0.19.5
Pillow>=11.3.0

# Agent runtime deps (must mirror flowra_busy_agent.py's imports)
requests>=2.32.3
schedule>=1.2.2
python-dotenv>=1.0.1
cryptography>=44.0.0

# Busy database access — pyodbc talks to Busy's Btrieve/Pervasive ODBC driver
# to read the customer's local .bds files. The driver itself is installed on
# every Busy Windows install; we only ship the Python bindings.
pyodbc>=5.1.0

# v1.4 — pywin32 gives us COM/ADODB access to Busy's official BSSData OLE DB
# provider, which uses the user's normal Busy login (no -1905 password issue).
# Windows-only; ignored on Linux dev machines.
pywin32>=306; sys_platform == "win32"

