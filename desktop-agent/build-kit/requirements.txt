# Runtime + build dependencies. build.bat installs these into a local venv.
# Pinned versions known to work with the agent v9.8.9.

# Build tools
pyinstaller==6.10.0

# Agent runtime deps (must mirror the agent's imports)
requests==2.32.3
xmltodict==0.13.0
schedule==1.2.2
python-dotenv==1.0.1
cryptography==43.0.1
websockets==12.0
